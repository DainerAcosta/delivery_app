import 'package:flutter/material.dart';

import 'material_color_generator.dart';

const primaryColor = Color(0xff00BFA6);
final MaterialColor primaryColorMaterial = generateMaterialColor(primaryColor);
const greyColor = Color(0xffFDFDFD);
